{
  "name": "demoblaze",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
     "test:all:chrome": "npx playwright test --project=chromium",
     "test:flow1:signUp": "npx playwright test -g 'Sign up' --headed --project=chromium",
     "test:flow2:addElementToCart": "npx playwright test -g 'Add element to cart' --headed --project=chromium",
     "test:flow3:completePurchase": "npx playwright test -g 'Complete purchase' --headed --project=chromium"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@faker-js/faker": "^9.8.0",
    "@playwright/test": "^1.53.1",
    "@types/node": "^24.0.4"
  }
}
